let text = 'UGCAA'
const dnaconvert = ( input ) => {
    str = str.replace (/[\+\-\*]/g, (a) => {
        if(a==="+"){return "PLUS"};
        if(a==="-"){return "MINUS"};
        if(a==="*"){return "STAR"};
    })
    return result
}
